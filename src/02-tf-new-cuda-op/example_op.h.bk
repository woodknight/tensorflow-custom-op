#ifndef __EXAMPLE_OP_H__
#define __EXAMPLE_OP_H__

template <typename Device, typename T>
struct ExampleFunctor
{
    void operator()(const Device &d, int size, const T *n, T *out);
};

#if GOOGLE_CUDA
// partially specialize functor for GpuDevice
template <typename Eigen::GpuDevice, typename T>
struct ExampleFunctor
{
    void operator()(const Eigen::Gpu_Device &d, int size, const T *n, T *out);
};


#endif

#endif // __EXAMPLE_OP_H__
